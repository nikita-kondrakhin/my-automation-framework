package Elements;

import Base.BaseElement;
import org.openqa.selenium.By;

public class TableRow extends BaseElement {
    public TableRow(By uniqueLocator, String elementName) {
        super(uniqueLocator, elementName);
    }
}
